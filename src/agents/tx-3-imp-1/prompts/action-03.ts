// AUTO-GENERATED by AIVIC contract-module-scaffolder
// module: src/agents/tx-3-imp-1/prompts/action-03.ts

import type { Tx3Imp1AiClient } from "../orchestrator";

export const ACTION_03_PROMPT_VERSION = "1.0.0";

export interface Action03PromptInput {
  confirmationEmailContent: string;
  reportingDeadline: string;
  currentTimestamp: string;
  escalationThreshold?: number;
}

export interface Action03PromptOutput {
  identifiedNonReporters: Array<{
    employeeId: string;
    employeeName: string;
    status: "non-reported" | "delayed";
    daysSinceDeadline: number;
  }>;
  escalationTargets: Array<{
    employeeId: string;
    employeeName: string;
    reason: string;
    priority: "high" | "medium" | "low";
  }>;
  actionPlan: string;
}

export function buildAction03Prompt(
  input: Action03PromptInput
): string {
  const {
    confirmationEmailContent,
    reportingDeadline,
    currentTimestamp,
    escalationThreshold = 2,
  } = input;

  return `You are an AI agent responsible for identifying non-reporters and delayed reporters from confirmation email content, and determining escalation targets.

## Task: Identify Non-Reporters and Determine Escalation Targets

### Input Information:
- Confirmation Email Content:
${confirmationEmailContent}

- Reporting Deadline: ${reportingDeadline}
- Current Timestamp: ${currentTimestamp}
- Escalation Threshold (days): ${escalationThreshold}

### Your Responsibilities:

1. **Identify Non-Reporters and Delayed Reporters**
   - Parse the confirmation email content to identify which employees have not submitted reports
   - Identify which employees have submitted reports but are delayed
   - Calculate days since deadline for each delayed reporter

2. **Determine Escalation Targets**
   - Identify employees who should receive escalation notifications
   - Prioritize based on:
     - Days overdue (higher priority for longer delays)
     - Previous escalation history if available
     - Severity of delay relative to threshold

3. **Generate Action Plan**
   - Specify which employees need reminder emails
   - Specify which employees need chat notifications
   - Recommend follow-up actions for repeated non-reporters

### Output Format:
Return a JSON object with the following structure:
{
  "identifiedNonReporters": [
    {
      "employeeId": "string",
      "employeeName": "string",
      "status": "non-reported" | "delayed",
      "daysSinceDeadline": number
    }
  ],
  "escalationTargets": [
    {
      "employeeId": "string",
      "employeeName": "string",
      "reason": "string",
      "priority": "high" | "medium" | "low"
    }
  ],
  "actionPlan": "string describing recommended actions"
}

### Constraints:
- Only identify employees who have genuinely not reported or are significantly delayed
- Avoid false positives that could lead to unnecessary escalations
- Prioritize high-priority escalations for immediate action
- Provide clear reasoning for each escalation decision`;
}
