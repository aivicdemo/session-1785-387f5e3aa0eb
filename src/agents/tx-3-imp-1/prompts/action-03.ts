// AUTO-GENERATED by AIVIC contract-module-scaffolder
// module: src/agents/tx-3-imp-1/prompts/action-03.ts

export const ACTION_03_PROMPT_VERSION = "1.0.0";

export interface Action03PromptInput {
  confirmationEmailContent: string;
  reportingDeadline: string;
  currentTimestamp: string;
  escalationThreshold: number;
}

export interface Action03PromptOutput {
  missingReporters: Array<{
    employeeId: string;
    employeeName: string;
    department: string;
    lastReminderTime?: string;
    reminderCount: number;
  }>;
  delayedReporters: Array<{
    employeeId: string;
    employeeName: string;
    department: string;
    submissionTime: string;
    delayMinutes: number;
  }>;
  escalationCandidates: Array<{
    employeeId: string;
    employeeName: string;
    reason: string;
    priority: "high" | "medium" | "low";
  }>;
  actionSummary: {
    totalEmployees: number;
    submittedCount: number;
    missingCount: number;
    delayedCount: number;
    escalationCount: number;
  };
}

export function buildAction03Prompt(input: Action03PromptInput): string {
  const {
    confirmationEmailContent,
    reportingDeadline,
    currentTimestamp,
    escalationThreshold,
  } = input;

  return `# Action 03: 報告漏れ・遅延部員の特定と催促対象判定

## 目的
確認メール内容から報告漏れ・遅延部員を自動特定し、催促対象を判定する。

## 入力情報
- 確認メール内容: ${confirmationEmailContent}
- 報告期限: ${reportingDeadline}
- 現在時刻: ${currentTimestamp}
- 催促判定閾値（分）: ${escalationThreshold}

## 実行タスク

### 1. 報告漏れ部員の特定
確認メール内容から以下を抽出:
- 未提出者のリスト（社員ID、名前、部門）
- 前回の催促時刻と催促回数
- 報告漏れの理由（システム障害、入力忘れなど）

### 2. 遅延部員の特定
以下の条件で遅延者を判定:
- 報告期限を超過した提出者
- 提出時刻と遅延時間を記録
- 遅延の程度を分類（軽微: 5分以内、中程度: 5-30分、重大: 30分以上）

### 3. 催促対象の判定
以下のルールで催促対象を判定:
- 未提出者: 全員を催促対象
- 遅延者: 遅延時間が${escalationThreshold}分以上の場合のみ催促対象
- 複数回催促済み: 前回催促から1時間以上経過した場合のみ再催促対象
- 同一部員への催促回数上限: 3回まで

### 4. エスカレーション候補の抽出
以下の場合はエスカレーション対象:
- 同一部員が3回以上催促されている
- システムエラーが原因の報告漏れ
- 特殊ケース（休暇中、出張中など）

## 出力形式
JSON形式で以下を返却:
\`\`\`json
{
  "missingReporters": [
    {
      "employeeId": "string",
      "employeeName": "string",
      "department": "string",
      "lastReminderTime": "ISO8601 or null",
      "reminderCount": number
    }
  ],
  "delayedReporters": [
    {
      "employeeId": "string",
      "employeeName": "string",
      "department": "string",
      "submissionTime": "ISO8601",
      "delayMinutes": number
    }
  ],
  "escalationCandidates": [
    {
      "employeeId": "string",
      "employeeName": "string",
      "reason": "string",
      "priority": "high" | "medium" | "low"
    }
  ],
  "actionSummary": {
    "totalEmployees": number,
    "submittedCount": number,
    "missingCount": number,
    "delayedCount": number,
    "escalationCount": number
  }
}
\`\`\`

## 注意事項
- 誤検知を防ぐため、システムエラーの可能性がある場合は理由を明記する
- 催促回数の上限に達した部員は別途エスカレーション対象とする
- 遅延時間の計算は秒単位で正確に行う
`;
}
