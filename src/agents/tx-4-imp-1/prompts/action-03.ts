// AUTO-GENERATED by AIVIC contract-module-scaffolder
// module: src/agents/tx-4-imp-1/prompts/action-03.ts

const ACTION_03_PROMPT_VERSION = "1.0.0";

interface Action03PromptInput {
  reportContent: string;
  employeeId: string;
  employeeName: string;
  departmentId: string;
  departmentName: string;
  reportDate: string;
  previousIssues?: Array<{
    id: string;
    title: string;
    description: string;
    status: string;
  }>;
}

interface Action03PromptOutput {
  version: string;
  systemPrompt: string;
  userPrompt: string;
  context: {
    employeeId: string;
    employeeName: string;
    departmentId: string;
    departmentName: string;
    reportDate: string;
  };
}

function buildAction03Prompt(input: Action03PromptInput): Action03PromptOutput {
  const systemPrompt = `You are an AI agent responsible for extracting issues and bottlenecks from daily reports in a morning meeting management system.

Your task is to:
1. Analyze the provided daily report content
2. Identify and extract all issues, blockers, and bottlenecks mentioned
3. Assess the severity and impact of each issue
4. Categorize issues by type (technical, resource, process, external dependency, etc.)
5. Provide clear, actionable descriptions for each extracted issue

Output format:
- Issue ID (auto-generated)
- Title (concise, max 10 words)
- Description (detailed explanation)
- Category (technical/resource/process/external/other)
- Severity (critical/high/medium/low)
- Impact area (team/project/timeline/quality/other)
- Recommended action (brief suggestion)

Be thorough but avoid over-interpretation. Only extract issues that are explicitly mentioned or clearly implied in the report.`;

  const previousIssuesContext =
    input.previousIssues && input.previousIssues.length > 0
      ? `\n\nPrevious Issues Context:\n${input.previousIssues
          .map(
            (issue) =>
              `- [${issue.id}] ${issue.title} (Status: ${issue.status})\n  ${issue.description}`
          )
          .join("\n")}`
      : "";

  const userPrompt = `Extract and analyze issues from the following daily report:

Employee: ${input.employeeName} (ID: ${input.employeeId})
Department: ${input.departmentName} (ID: ${input.departmentId})
Report Date: ${input.reportDate}

Report Content:
${input.reportContent}${previousIssuesContext}

Please extract all issues, blockers, and bottlenecks from this report. For each issue, provide the structured information as specified in your instructions.`;

  return {
    version: ACTION_03_PROMPT_VERSION,
    systemPrompt,
    userPrompt,
    context: {
      employeeId: input.employeeId,
      employeeName: input.employeeName,
      departmentId: input.departmentId,
      departmentName: input.departmentName,
      reportDate: input.reportDate,
    },
  };
}

export { buildAction03Prompt, ACTION_03_PROMPT_VERSION };
