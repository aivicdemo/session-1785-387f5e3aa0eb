// AUTO-GENERATED by AIVIC contract-module-scaffolder
// module: src/agents/tx-3-imp-1/prompts/action-03.ts

export const ACTION_03_PROMPT_VERSION = "1.0.0";

export interface Action03PromptInput {
  confirmationEmailContent: string;
  reportingDeadline: string;
  currentTimestamp: string;
  escalationThreshold: number;
}

export interface Action03PromptOutput {
  missingReporters: Array<{
    employeeId: string;
    employeeName: string;
    department: string;
    lastReminderTime?: string;
    reminderCount: number;
  }>;
  delayedReporters: Array<{
    employeeId: string;
    employeeName: string;
    department: string;
    submissionTime: string;
    delayMinutes: number;
  }>;
  escalationCandidates: Array<{
    employeeId: string;
    employeeName: string;
    department: string;
    reason: string;
    priority: "high" | "medium" | "low";
  }>;
  summary: {
    totalEmployees: number;
    submittedCount: number;
    missingCount: number;
    delayedCount: number;
    escalationCount: number;
  };
}

export function buildAction03Prompt(input: Action03PromptInput): string {
  const systemPrompt = `You are an AI agent responsible for identifying missing and delayed reporters from confirmation email content.

Your task is to:
1. Parse the confirmation email content to identify which employees have submitted their reports
2. Identify employees who have NOT submitted reports (missing reporters)
3. Identify employees who submitted reports AFTER the deadline (delayed reporters)
4. Determine which cases require escalation based on the escalation threshold
5. Return structured data with all identified issues

Current timestamp: ${input.currentTimestamp}
Reporting deadline: ${input.reportingDeadline}
Escalation threshold (reminder count): ${input.escalationThreshold}

Confirmation email content to analyze:
${input.confirmationEmailContent}

Return your analysis in the following JSON format:
{
  "missingReporters": [
    {
      "employeeId": "string",
      "employeeName": "string",
      "department": "string",
      "lastReminderTime": "ISO8601 string or null",
      "reminderCount": number
    }
  ],
  "delayedReporters": [
    {
      "employeeId": "string",
      "employeeName": "string",
      "department": "string",
      "submissionTime": "ISO8601 string",
      "delayMinutes": number
    }
  ],
  "escalationCandidates": [
    {
      "employeeId": "string",
      "employeeName": "string",
      "department": "string",
      "reason": "string",
      "priority": "high" | "medium" | "low"
    }
  ],
  "summary": {
    "totalEmployees": number,
    "submittedCount": number,
    "missingCount": number,
    "delayedCount": number,
    "escalationCount": number
  }
}

Be precise and accurate in your identification. Only include employees that are clearly identified in the email content.`;

  return systemPrompt;
}
