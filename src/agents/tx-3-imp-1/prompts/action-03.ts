// AUTO-GENERATED by AIVIC contract-module-scaffolder
// module: src/agents/tx-3-imp-1/prompts/action-03.ts

import type { Tx3Imp1AiClient } from '../orchestrator';

export const ACTION_03_PROMPT_VERSION = '1.0.0';

export interface Action03PromptInput {
  confirmationEmailContent: string;
  reportingDeadline: string;
  currentTimestamp: string;
  escalationThreshold?: number;
}

export interface Action03PromptOutput {
  identifiedNonReporters: Array<{
    employeeId: string;
    employeeName: string;
    status: 'non_reported' | 'delayed';
    daysSinceDeadline: number;
  }>;
  escalationTargets: Array<{
    employeeId: string;
    employeeName: string;
    reason: string;
    priority: 'high' | 'medium' | 'low';
  }>;
  actionPlan: string;
}

export function buildAction03Prompt(input: Action03PromptInput): string {
  const {
    confirmationEmailContent,
    reportingDeadline,
    currentTimestamp,
    escalationThreshold = 2,
  } = input;

  return `You are an AI agent responsible for identifying non-reporters and delayed reporters from confirmation email content, and determining escalation targets.

## Task: Identify Non-Reporters and Determine Escalation Targets

### Input Information:
- Confirmation Email Content:
${confirmationEmailContent}

- Reporting Deadline: ${reportingDeadline}
- Current Timestamp: ${currentTimestamp}
- Escalation Threshold (days): ${escalationThreshold}

### Your Responsibilities:

1. **Parse the confirmation email content** to identify:
   - Employees who have NOT submitted their reports (non_reported status)
   - Employees whose reports are delayed beyond the deadline (delayed status)
   - Calculate days since deadline for delayed reporters

2. **Determine escalation targets** based on:
   - Days overdue exceeding the escalation threshold
   - Pattern of repeated non-submission
   - Severity of delay

3. **Assign priority levels**:
   - HIGH: Multiple days overdue or repeated offender
   - MEDIUM: 1-2 days overdue, first-time delay
   - LOW: Just past deadline, minor delay

4. **Generate action plan** describing:
   - Which employees require immediate follow-up
   - Recommended communication method (email/chat)
   - Suggested messaging tone

### Output Format:
Return a JSON object with the following structure:
{
  "identifiedNonReporters": [
    {
      "employeeId": "string",
      "employeeName": "string",
      "status": "non_reported" | "delayed",
      "daysSinceDeadline": number
    }
  ],
  "escalationTargets": [
    {
      "employeeId": "string",
      "employeeName": "string",
      "reason": "string",
      "priority": "high" | "medium" | "low"
    }
  ],
  "actionPlan": "string describing recommended actions"
}

### Constraints:
- Only identify employees explicitly mentioned in the confirmation email
- Calculate days accurately based on provided timestamps
- Prioritize high-priority escalations
- Ensure action plan is specific and actionable`;
}
