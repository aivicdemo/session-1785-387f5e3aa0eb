// AUTO-GENERATED by AIVIC contract-module-scaffolder
// module: src/agents/tx-1-imp-1/prompts/action-05.ts

/**
 * Action 05: 管理者に確認メールを自動配信する
 * 
 * 日報が管理システムに登録された後、管理者に対して確認メールを自動配信するプロンプトを生成します。
 * このアクションは日報送信処理の完了を管理者に通知し、確認・レビューの開始を促します。
 */

export const ACTION_05_PROMPT_VERSION = "1.0.0";

export interface Action05PromptInput {
  engineerName: string;
  engineerEmail: string;
  reportDate: string;
  reportContent: {
    yesterday: string;
    today: string;
    issues: string;
  };
  adminEmails: string[];
  systemUrl: string;
  reportId: string;
}

export interface Action05PromptOutput {
  emailSubject: string;
  emailBody: string;
  recipientEmails: string[];
  sendTimestamp: string;
  reportSummary: string;
}

export function buildAction05Prompt(input: Action05PromptInput): string {
  const formattedDate = new Date(input.reportDate).toLocaleDateString("ja-JP", {
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
  });

  const prompt = `あなたは朝会報告管理システムの自動配信エージェントです。
以下の日報が管理システムに正常に登録されました。
管理者に対して確認メールを自動配信するための内容を生成してください。

【登録された日報情報】
- エンジニア名: ${input.engineerName}
- メールアドレス: ${input.engineerEmail}
- 報告日: ${formattedDate}
- 報告ID: ${input.reportId}

【日報内容】
昨日の実績:
${input.reportContent.yesterday}

本日の予定:
${input.reportContent.today}

抱えている課題:
${input.reportContent.issues}

【配信先管理者】
${input.adminEmails.join(", ")}

【システムURL】
${input.systemUrl}

以下の形式で確認メールの内容を生成してください:

1. メール件名: 簡潔で、日報登録完了と確認対象者を明記したもの
2. メール本文: 
   - 日報登録完了の通知
   - エンジニア名と報告日
   - 日報内容の要約（昨日の実績、本日の予定、課題を簡潔に記載）
   - システムURL内での確認方法
   - 確認期限（本日中の確認を推奨）
3. 送信対象メールアドレス: 指定された管理者全員
4. 送信タイムスタンプ: 現在時刻（ISO 8601形式）
5. 日報サマリー: 課題の有無と重要度レベルの判定

メール本文は、管理者が迅速に内容を把握でき、システムで確認できるよう、簡潔かつ構造化された形式で作成してください。`;

  return prompt;
}
