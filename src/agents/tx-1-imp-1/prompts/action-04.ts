// AUTO-GENERATED by AIVIC contract-module-scaffolder
// module: src/agents/tx-1-imp-1/prompts/action-04.ts

/**
 * Action 4: 日報を管理システムに登録する
 * 
 * Contract: tx_1_imp_1
 * Orchestrator: src/agents/tx-1-imp-1/orchestrator.ts
 * 
 * このアクションは、エンジニアから受け取った日報入力内容を検証後、
 * 管理システムに登録するためのプロンプトを生成します。
 */

export const ACTION_04_PROMPT_VERSION = "1.0.0";

export interface Action04PromptInput {
  engineerId: string;
  engineerName: string;
  reportDate: string;
  yesterdayAccomplishments: string;
  todayPlans: string;
  currentIssues: string;
  validationResults: {
    isComplete: boolean;
    isAppropriate: boolean;
    issues: string[];
  };
}

export interface Action04PromptOutput {
  systemPrompt: string;
  userPrompt: string;
}

export function buildAction04Prompt(
  input: Action04PromptInput
): Action04PromptOutput {
  const systemPrompt = `You are an AI assistant responsible for registering daily reports into the management system.
Your role is to:
1. Verify that the report data is complete and valid
2. Format the report data according to system requirements
3. Prepare the report for registration in the management system
4. Generate a registration confirmation message

You must ensure data integrity and consistency before registration.
Always validate that all required fields are present and properly formatted.`;

  const validationStatus = input.validationResults.isComplete
    ? "✓ Complete"
    : "✗ Incomplete";
  const appropriatenessStatus = input.validationResults.isAppropriate
    ? "✓ Appropriate"
    : "✗ Inappropriate";
  const issuesText =
    input.validationResults.issues.length > 0
      ? `Issues found:\n${input.validationResults.issues.map((issue) => `- ${issue}`).join("\n")}`
      : "No issues found";

  const userPrompt = `Register the following daily report into the management system:

Engineer ID: ${input.engineerId}
Engineer Name: ${input.engineerName}
Report Date: ${input.reportDate}

Report Content:
- Yesterday's Accomplishments: ${input.yesterdayAccomplishments}
- Today's Plans: ${input.todayPlans}
- Current Issues: ${input.currentIssues}

Validation Status:
- Completeness: ${validationStatus}
- Appropriateness: ${appropriatenessStatus}
${issuesText}

Please:
1. Confirm the report is ready for registration
2. Format the data for system storage
3. Generate a registration ID or confirmation code
4. Prepare a confirmation message for the engineer`;

  return {
    systemPrompt,
    userPrompt,
  };
}
