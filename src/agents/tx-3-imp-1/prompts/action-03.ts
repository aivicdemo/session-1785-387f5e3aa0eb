// AUTO-GENERATED by AIVIC contract-module-scaffolder
// module: src/agents/tx-3-imp-1/prompts/action-03.ts

import type { Tx3Imp1AiClient } from "../orchestrator";

export const ACTION_03_PROMPT_VERSION = "1.0.0";

export interface Action03PromptInput {
  confirmationEmailContent: string;
  reportingDeadline: string;
  currentTimestamp: string;
  previousReminders?: Array<{
    employeeId: string;
    reminderCount: number;
    lastReminderTime: string;
  }>;
  reminderRules?: {
    maxRemindersPerEmployee: number;
    reminderIntervalMinutes: number;
  };
}

export interface Action03PromptOutput {
  identifiedNonReporters: Array<{
    employeeId: string;
    employeeName: string;
    reason: "not_submitted" | "delayed";
    daysSinceDeadline?: number;
  }>;
  remindersToSend: Array<{
    employeeId: string;
    employeeName: string;
    reminderType: "email" | "chat" | "both";
    message: string;
    priority: "normal" | "high";
  }>;
  escalationCases: Array<{
    employeeId: string;
    employeeName: string;
    reason: string;
    requiresManualReview: boolean;
  }>;
  executionSummary: {
    totalIdentified: number;
    remindersScheduled: number;
    escalationsDetected: number;
  };
}

export function buildAction03Prompt(
  input: Action03PromptInput
): string {
  const reminderRules = input.reminderRules || {
    maxRemindersPerEmployee: 3,
    reminderIntervalMinutes: 60,
  };

  const previousRemindersInfo =
    input.previousReminders && input.previousReminders.length > 0
      ? `\n\n## 過去の催促履歴\n${input.previousReminders
          .map(
            (r) =>
              `- 従業員ID: ${r.employeeId}, 催促回数: ${r.reminderCount}回, 最終催促時刻: ${r.lastReminderTime}`
          )
          .join("\n")}`
      : "";

  return `# Action 03: 報告漏れ特定から催促送信までの自動実行

## タスク概要
確認メール内容から報告漏れ・遅延部員を自動特定し、催促対象を判定して、メール・チャットの送信まで完結させます。

## 入力情報

### 確認メール内容
\`\`\`
${input.confirmationEmailContent}
\`\`\`

### 報告期限
${input.reportingDeadline}

### 現在時刻
${input.currentTimestamp}

### 催促ルール
- 従業員あたりの最大催促回数: ${reminderRules.maxRemindersPerEmployee}回
- 催促間隔: ${reminderRules.reminderIntervalMinutes}分${previousRemindersInfo}

## 実行ステップ

### ステップ1: 報告漏れ・遅延部員の特定
確認メール内容を解析し、以下の情報を抽出してください:
- 報告未提出者のリスト（従業員ID、名前）
- 報告遅延者のリスト（従業員ID、名前、期限超過日数）
- 各部員の報告状況（提出済み、未提出、遅延）

### ステップ2: 催促対象部員の判定
以下の条件に基づいて催促対象を判定してください:
- 報告未提出者は全員催促対象
- 報告遅延者のうち、期限超過が1時間以上の場合は催促対象
- 過去の催促回数が最大回数に達していないこと
- 前回催促から指定間隔以上経過していること

### ステップ3: 催促メール・チャットの作成
各催促対象部員に対して、以下の内容を含むメッセージを作成してください:
- 丁寧で適切な催促文言
- 報告期限の再通知
- 報告方法の案内
- 緊急度に応じた優先度設定

### ステップ4: エスカレーションケースの特定
以下の場合はエスカレーションとして標記してください:
- 同一部員への複数回催促後も報告がない場合
- システムエラーでメール・チャット送信に失敗した場合
- 催促ルールに該当しない特殊ケース

## 出力形式

JSON形式で以下の構造で返してください:

\`\`\`json
{
  "identifiedNonReporters": [
    {
      "employeeId": "string",
      "employeeName": "string",
      "reason": "not_submitted" | "delayed",
      "daysSinceDeadline": number (遅延の場合のみ)
    }
  ],
  "remindersToSend": [
    {
      "employeeId": "string",
      "employeeName": "string",
      "reminderType": "email" | "chat" | "both",
      "message": "string",
      "priority": "normal" | "high"
    }
  ],
  "escalationCases": [
    {
      "employeeId": "string",
      "employeeName": "string",
      "reason": "string",
      "requiresManualReview": boolean
    }
  ],
  "executionSummary": {
    "totalIdentified": number,
    "remindersScheduled": number,
    "escalationsDetected": number
  }
}
\`\`\`

## 重要な注意事項
- 送信履歴を保存し、誤送信時に取り消し・修正できる仕組みを考慮してください
- 催促回数の上限を設定して過度な催促を防いでください
- 部長への例外案件のみ確認するため、エスカレーション判定は厳密に行ってください
`;
}
