// AUTO-GENERATED by AIVIC contract-module-scaffolder
// module: src/agents/tx-4-imp-1/prompts/action-02.ts

const ACTION_02_PROMPT_VERSION = "1.0.0";

interface Action02PromptContext {
  engineerName: string;
  reportDate: string;
  previousReportContent?: string;
  submissionDeadline: string;
  systemName: string;
}

interface Action02PromptResult {
  version: string;
  action: string;
  systemPrompt: string;
  userPrompt: string;
  context: Action02PromptContext;
}

function buildAction02Prompt(context: Action02PromptContext): Action02PromptResult {
  const systemPrompt = `You are an AI agent responsible for receiving and validating engineer daily reports in the morning meeting report management system.

Your role is to:
1. Receive engineer input content (yesterday's achievements, today's plans, current issues)
2. Validate the completeness and appropriateness of the input
3. Ensure all required fields are filled with meaningful content
4. Detect any anomalies or concerning patterns in the report content
5. Prepare the report for registration in the management system

Validation rules:
- All three sections (achievements, plans, issues) must be present and non-empty
- Content should be specific and actionable, not generic or placeholder text
- Issue descriptions should clearly articulate the problem and its impact
- Report should be submitted before the deadline: ${context.submissionDeadline}

When validation fails, provide clear feedback on what needs to be corrected.
When validation succeeds, confirm that the report is ready for system registration.`;

  const userPrompt = `Process the daily report submission for engineer: ${context.engineerName}
Report date: ${context.reportDate}
System: ${context.systemName}

${context.previousReportContent ? `Previous report for reference:\n${context.previousReportContent}\n` : ""}

Please validate the submitted report content and confirm its readiness for registration in the management system.`;

  return {
    version: ACTION_02_PROMPT_VERSION,
    action: "action-02",
    systemPrompt,
    userPrompt,
    context,
  };
}

export { buildAction02Prompt, ACTION_02_PROMPT_VERSION };
