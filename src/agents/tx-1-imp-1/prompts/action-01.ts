// AUTO-GENERATED by AIVIC contract-module-scaffolder
// module: src/agents/tx-1-imp-1/prompts/action-01.ts

const ACTION_01_PROMPT_VERSION = "1.0.0";

interface Action01PromptInput {
  engineerName: string;
  engineerEmail: string;
  previousDayDate: string;
  currentDayDate: string;
  submissionDeadline: string;
}

interface Action01PromptOutput {
  version: string;
  systemPrompt: string;
  userPrompt: string;
  templateContent: string;
}

function buildAction01Prompt(input: Action01PromptInput): Action01PromptOutput {
  const systemPrompt = `You are an AI assistant responsible for generating and distributing daily report templates for engineers. Your role is to:
1. Create a structured daily report template with sections for yesterday's achievements, today's plans, and current issues
2. Personalize the template for each engineer
3. Ensure the template is clear and easy to fill out
4. Include the submission deadline and any relevant instructions

The daily report system aims to automate the collection and management of engineer progress reports for morning meetings.`;

  const userPrompt = `Generate a daily report template for the following engineer:
- Name: ${input.engineerName}
- Email: ${input.engineerEmail}
- Report Date: ${input.currentDayDate}
- Previous Day: ${input.previousDayDate}
- Submission Deadline: ${input.submissionDeadline}

The template should include:
1. Yesterday's Achievements section
2. Today's Plans section
3. Current Issues/Blockers section
4. Additional Notes section

Format the template in a clear, structured manner that can be easily filled out and processed by the system.`;

  const templateContent = `=== Daily Report Template ===
Engineer: ${input.engineerName}
Email: ${input.engineerEmail}
Report Date: ${input.currentDayDate}
Submission Deadline: ${input.submissionDeadline}

--- Yesterday's Achievements (${input.previousDayDate}) ---
[Please describe what you accomplished yesterday]

--- Today's Plans (${input.currentDayDate}) ---
[Please describe your planned tasks for today]

--- Current Issues/Blockers ---
[Please describe any issues or blockers you are facing]

--- Additional Notes ---
[Any other relevant information]

=== End of Template ===`;

  return {
    version: ACTION_01_PROMPT_VERSION,
    systemPrompt,
    userPrompt,
    templateContent,
  };
}

export { buildAction01Prompt, ACTION_01_PROMPT_VERSION };
